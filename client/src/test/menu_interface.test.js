import React from 'react';
import renderer from 'react-test-renderer';
import { shallow, mount, render } from 'enzyme';
import menu_interface from '../menu_interface';

// test rendering of components
describe('', () => {
  it('should render without throwing an error', () => {
    //expect<shallow>
  });
});

// test links on buttons
describe('', () => {
  it('should build cat album component', () => {

  });
  it('should build shop component', () => {

  });
  // it('should build inventory component', () => {

  // });
  it('should go to yard', () => {

  });
  // it('should build settings component', () => {

  // });
});